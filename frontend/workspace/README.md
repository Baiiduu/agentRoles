# Education Workspace Frontend

启动：

```powershell
npm install
npm run dev
```

默认读取：

```text
VITE_API_BASE_URL
```

当前先迁入：

1. `Agent Playground`

其余页面先保留占位：

1. `Dashboard`
2. `Case Workspace`
3. `Workflow Studio`
4. `Teacher Console`
