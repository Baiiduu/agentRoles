# Docs

`docs/` 目录只保留两类文档：

1. `architecture/`
   架构、设计、实现方案与模块边界文档。
2. `github/`
   GitHub 协作流程、Review 规则与仓库配置文档。

维护约定：

1. 新增文档时，优先判断它属于 `architecture` 还是 `github`。
2. 如果新增第三类内容，先确认是否真的需要单独分类，再落目录。
3. 新增子目录时补一个 `README.md`，说明目录职责和主要入口。
